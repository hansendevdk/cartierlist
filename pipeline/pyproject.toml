[project]
name = "cartierlist-pipeline"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.5",
    "lxml>=6.1.1",
    "pyarrow>=25.0.0",
    "zipfile-deflate64>=0.2.0",
]
