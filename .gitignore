# raw and intermediate data — large, not source, not pushed to git
data/raw/
data/interim/
data/warehouse.duckdb
data/warehouse.duckdb.wal
# same rule, one directory deeper: data/raw/ above is anchored to the repo
# root by its own inner slash and does not reach pipeline/data/raw/, so the
# scraped DBA/Bilbasen raw cache needs its own line.
pipeline/data/raw/dba_bilbasen/
*.zip
*.parquet

# python
.venv/
__pycache__/
*.pyc

# node / astro (later phases)
node_modules/
dist/
.astro/

# local secrets
.env

# claude code harness state
.claude/
.serena/

# working notes, not part of the site's published methodology docs
reports/handover_*.md
